{
  "name": "mcp-echo",
  "version": "1.0.0",
  "description": "A simple MCP server that echoes text messages",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && copyfiles -u 0 credentials.json token.json dist/",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mcp",
    "echo",
    "server",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2",
    "googleapis": "^148.0.0"
  },
  "devDependencies": {
    "@types/node": "^18.15.11",
    "copyfiles": "^2.4.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.8.3"
  }
}
